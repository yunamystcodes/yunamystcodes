name: Add YunaMyst social links

# Os links sociais já estão no index.html. Este workflow fica manual para não
# disputar alterações com a atualização automática dos códigos.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verificar configuração
        run: echo "Links sociais mantidos diretamente no index.html."
