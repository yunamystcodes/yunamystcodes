name: Sync Summoners War codes

# A atualização do index agora é feita por update_codes.py.
# Este workflow fica apenas manual para não haver duas rotinas a escrever no index ao mesmo tempo.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verificar configuração
        run: echo "A sincronização automática é feita por scripts/update_codes.py."
